<input @bind:get="Value" @bind:set="ValueChanged" @bind:event="oninput"/>

@code {
    [Parameter]
    public string? Value { get; set; }

    [Parameter]
    public EventCallback<string?> ValueChanged { get; set; }
}
